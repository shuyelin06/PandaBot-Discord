// This command will be to create a queue
module.exports = {
    name: 'play',
    description: 'Check if the bot is online',
    aliases: ['p'],
    execute(msg, args){
        msg.reply("This command is still being worked on!");
    }
}